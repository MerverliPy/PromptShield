# Current quick start works without a .env file.
#
# Local defaults already used by the workspace:
# - dashboard: Next.js dev server default http://localhost:3000
# - proxy: http://localhost:4002
# - optimizer: http://localhost:4003
#
# Optional optimizer override when running that service directly:
# PORT=4003
