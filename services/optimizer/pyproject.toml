[project]
name = "promptshield-optimizer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic"
]

[project.optional-dependencies]
test = [
  "httpx",
  "pytest",
]
